# frontend-to-workflow

## Agent skills

### Issue tracker

Issues and PRDs are tracked in this repo's GitHub Issues (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, each label string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — a root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
