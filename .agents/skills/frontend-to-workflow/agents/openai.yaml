interface:
  display_name: "Frontend To Workflow"
  short_description: "Repo-specific patterns and workflows for frontend-to-workflow"
  default_prompt: "Use the frontend-to-workflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true